[project]
name = "test-zarr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "zarr",
    "pandas",
    "duckdb",
    "xarray",
    "pyarrow",
    "dask",
    "ruff",
]
